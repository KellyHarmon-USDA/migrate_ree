# Migration configuration for REE's spotlights (also called Items).
id: item
label: Spotlight migration
migration_group: REE
source:
  plugin: spotlights
destination:
  plugin: entity:node
process:
  nid: nid
  type:
    plugin: default_value
    default_value: spotlights
  title: title
migration_dependencies: {}
dependencies:
  enforced:
    module:
      - migrate_ree
